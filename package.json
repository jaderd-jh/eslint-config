{
  "name": "@jhqn/eslint-config-monorepo",
  "version": "0.4.11-beta.1",
  "private": true,
  "description": "jhqn's ESLint configs",
  "author": "jade-gjz",
  "packageManager": "pnpm@8.6.5",
  "scripts": {
    "lint": "eslint .",
    "update": "pnpm up -i --latest -r",
    "release": "bumpp -r",
    "//release": "bumpp package.json packages/*/package.json --commit --push --tag && pnpm -r publish --access public",
    "//postpublish": "conventional-changelog -p angular -i CHANGELOG.md -s -r 0 && git add CHANGELOG.md && git commit -am \"chore(docs): update CHANGELOG.md\" && git push"
  },
  "devDependencies": {
    "@jhqn/eslint-config-ts": "workspace:*",
    "bumpp": "^9.1.0",
    "conventional-changelog-cli": "^3.0.0",
    "eslint": "^8.41.0",
    "typescript": "^5.0.4"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "eslint"
      ]
    }
  }
}
